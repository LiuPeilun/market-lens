# Market Lens 项目规划与进展

> 本文档是项目规划、技术决策和实际进展的单一事实来源。开始新任务前应先阅读本文档；完成开发、验证或方案调整后，应同步更新本文档。

## 文档信息

- 最后更新：2026-07-21
- 当前阶段：估值数据与评分框架增强
- 当前工作包：V2-4 股票行业模型（下一步）
- 当前分支：`main`
- 最新提交基线：`ee6ede7 feat(valuation): add factor source diagnostics`
- 工作区验证：2026-07-21 后端测试 `154 passed, 6 skipped`，Ruff 通过；前端 lint 和 build 通过

## 项目目标

Market Lens 是面向个人投资研究的智能体项目。用户既可以通过代码或名称直接查询股票、基金和 ETF，也可以通过自然语言对话触发数据查询、确定性分析和工具调用。

项目输出用于研究辅助，不构成投资建议。LLM 负责理解问题和解释结果，估值计算、数据质量判断和风险控制必须由可测试的确定性代码完成。

## 核心原则

1. **先验证数据，再设计因子**：只有经过实际接口请求、字段语义检查和覆盖率验证的数据，才能进入正式评分。
2. **数据路由必须准确**：基金、ETF、ETF 联接基金和指数必须先解析真实跟踪关系；无法确认时应停止估值，不允许用错误持仓兜底。
3. **不同概念独立输出**：估值、基本面质量、产品质量和置信度不能混为一个未经解释的分数。
4. **缺失数据不猜测**：缺失因子应降低覆盖率和置信度，不由 LLM 补全，也不静默使用行业均值替代。
5. **行业采用专属模型**：银行、保险、券商、周期、成长和红利类资产不能共用完全相同的因子权重。
6. **所有结论可追溯**：输出应包含数据日期、来源、计算方法、因子值、覆盖率和降级原因。
7. **高风险工具必须受控**：MCP 和代码执行统一经过 ToolRegistry、风险策略、审批、审计与沙箱边界。

## 当前系统能力

| 模块 | 状态 | 当前能力 |
| --- | --- | --- |
| FastAPI 后端 | 已完成 | 搜索、股票/基金数据、估值分析、流式问答和历史记录 API |
| React 独立前端 | 已完成 | 查询、分析结果、走势图、聊天、登录和历史记录界面 |
| Supabase | 已完成第一阶段 | 用户认证、用户级分析与聊天历史、RLS migration |
| 股票数据 | 已接入 | 行情历史、估值历史、同板块横截面、行业、通用及金融行业财务指标、同行比较和分红 |
| 基金/ETF 数据 | 已接入基础路径 | 净值、场内价格、跟踪指数、目标 ETF、前十大持仓路由、费率和期末净资产 |
| 估值引擎 | 已完成 V2 基础设施 | 版本化因子配置、统一状态与样本门槛、维度置信度、顶层 `assessment`；旧估值字段继续兼容 |
| LLM 问答 | 已完成第一版 | OpenAI 兼容接口、流式输出、分析上下文和工具编排 |
| ToolRegistry 与审批 | 已完成 | 风险分级、审批状态机、调用限制和审计边界 |
| Docker 沙箱 | 已完成开发版 | 禁网、只读根文件系统、非 root 用户和资源限制 |
| Daytona 沙箱 | 已完成执行器 | 远程临时沙箱、资源限制、文件传输和清理 |
| MCP Gateway | 已完成基础版 | HTTPS Streamable HTTP、隔离 stdio、工具白名单和 DeepWiki 验证 |

## 当前估值实现

### 股票

当前股票综合估值分仅使用历史分位：

| 因子 | 当前权重 |
| --- | ---: |
| PE TTM | 40% |
| PB | 30% |
| PS TTM | 15% |
| PCF OCF TTM | 15% |

当前分数方向为：`0` 表示极度低估，`100` 表示极度高估。ROE、增长、同行比较和股息率目前只作为分析上下文，尚未进入正式股票评分。

### 基金与 ETF

- 普通基金目前使用持仓加权 PE/PB 历史分位、同行 PE 分位和股息率。
- 红利低波基金提高股息率权重。
- 指数/ETF 在无法获得指数估值历史时使用指数价格历史分位代理，该结果只能表示价格位置，不能等同于真实基本面估值。
- 当前稳定持仓路径通常只能获得前十大持仓。覆盖率不足时必须降低置信度，不能宣称代表完整基金组合。

## 数据可得性核验

以下结论基于 2026-07-21 对一般消费、成长、银行、保险、券商和公用事业代表性股票的实际接口请求。

### 已接入且可使用

| 数据 | 用途 | 备注 |
| --- | --- | --- |
| PE、PB、PS、PCF、PEG 与历史值 | 历史估值分位 | PE/PB/PS/PCF 已进入股票评分 |
| 股票价格、基金净值、ETF/指数价格历史 | 收益、回撤、波动和价格位置 | 需区分净值与场内价格 |
| 行业分类 | 模型路由和行业比较 | 已获取东方财富与证监会分类 |
| 同板块横截面 PE/PB 分位 | 行业相对估值上下文 | 已接入，只读展示，尚未进入评分 |
| ROE、扣非 ROE | 基本面质量 | 已解析，尚未计分 |
| 营收增长、净利润增长 | 成长质量 | 已解析，尚未计分 |
| 毛利率、净利率 | 盈利质量 | 已解析，尚未计分 |
| ROIC、年度 FCFF 源字段 | 通用非金融企业质量上下文 | 已只读接入；FCFF 不视为 TTM，暂不计算自由现金流收益率 |
| 银行净息差、不良率、拨备覆盖率、资本充足率 | 银行质量上下文 | 已只读接入，尚未计分 |
| 保险偿付能力、新业务价值及利润率 | 保险质量上下文 | 已只读接入，尚未计分 |
| 券商风险覆盖率、流动性覆盖率、净稳定资金率、净资本/净资产 | 券商质量上下文 | 已只读接入，尚未计分 |
| 近 12 个月实际股息率 | 红利估值 | 根据除权分红和价格计算 |
| 跟踪指数、目标 ETF、前十大持仓 | 基金路由与有限覆盖分析 | 必须保留来源、日期和覆盖率 |
| 基金管理费、托管费、销售服务费和期末净资产 | 基金产品质量上下文 | 已只读接入；规模使用 `ENDNAV` 并保留 `FEGMRQ` 报告日 |

### 尚待验证或接入

| 数据 | 适用范围 | 接入前要求 |
| --- | --- | --- |
| 跟踪误差和跟踪偏离 | 指数基金/ETF | 对齐基金与基准日收益率并统一收益指数口径 |
| 自由现金流收益率 | 非金融企业 | 当前 FCFF 路由是年度双字段，未确认 TTM 口径前不计算 |

### 当前不可作为核心因子

| 数据 | 原因 | 当前决策 |
| --- | --- | --- |
| EV/EBITDA | 当前数据路由没有可靠字段 | 第一版剔除 |
| 研发效率 | 当前数据不足以稳定计算 | 第一版剔除 |
| 保险 PEV | 缺少可靠的内含价值历史 | 第一版剔除，不以 NBV 替代 EV |
| ETF 实时买卖价差与折溢价 | 尚未完成可靠数据路由验证 | 暂不计分 |
| 指数 PE/PB/股息率历史 | 当前项目没有可靠历史路由 | 未接入前只输出价格位置代理 |
| 基金完整实时持仓 | 通常只有定期披露的前十大持仓 | 不支持高置信度完整组合估值 |

## 已确认的输出模型

最终输出拆分为四个独立维度：

| 维度 | 判断问题 | 分数方向 |
| --- | --- | --- |
| 估值分 | 当前价格相对自身历史和同行业贵不贵 | `0` 极低估，`100` 极高估 |
| 基本面质量分 | 盈利能力、经营稳定性和成长可持续性如何 | `0` 较差，`100` 较好 |
| 产品质量分 | 基金/ETF 的费率、规模、流动性和跟踪质量如何 | `0` 较差，`100` 较好；股票不适用 |
| 置信度 | 数据和模型对此结果的支持程度有多高 | `0` 无可靠结论，`100` 高可靠性 |

成长因子纳入基本面质量分，不再额外制造一个含义重叠的第五维度。产品质量不能改变底层资产估值，置信度也不能作为投资吸引力奖励项。

### 估值等级

| 估值分 | 等级 |
| ---: | --- |
| 0 至小于 15 | 极度低估 |
| 15 至小于 30 | 低估 |
| 30 至小于 45 | 正常估值偏下 |
| 45 至小于 60 | 正常估值 |
| 60 至小于 75 | 正常估值偏上 |
| 75 至小于 90 | 高估 |
| 90 至 100 | 极度高估 |

## 待回测假设：综合吸引力分

原“调整分”候选名称为“综合吸引力分”。它不是估值分，也不能覆盖四个独立维度，只能作为多标的研究排序假设。回测通过前不进入正式 API、前端或 LLM 结论。

以下公式仅作为待验证假设，不是生产权重：

```text
便宜程度 = 100 - 估值分

股票原始吸引力 = 便宜程度 * 60% + 基本面质量分 * 40%

基金/ETF 原始吸引力 =
    便宜程度 * 50%
  + 底层资产质量分 * 30%
  + 产品质量分 * 20%

置信度调整后吸引力 =
    50 + (原始吸引力 - 50) * 置信度
```

当置信度为 `0` 时，候选结果回归中性 `50`；置信度为 `1` 时，保留原始值。正式权重、等级和展示文案必须由 V2-7 回测确定，V2-8 才允许发布。任何综合分都必须与四个原始维度同时展示，不能映射为买入或卖出信号。

## 估值优化实施工作流

估值 V2 按“数据事实先于评分逻辑”的顺序实施。任何工作包都不得跨过数据验证直接修改生产权重。

### 单个因子的准入流程

1. **实时探测**：使用多个行业代表性标的请求真实接口，确认字段存在、非空覆盖、日期和单位。
2. **保存夹具**：将脱敏后的典型响应、空值响应和异常响应固化为测试夹具，不让单元测试依赖公网。
3. **建立类型**：在 `market_lens/types.py` 中定义明确字段，不让评分逻辑直接读取 `raw` 字典。
4. **解析与诊断**：数据适配器输出字段值，同时记录来源、日期、状态和降级原因。
5. **只读展示**：新字段先进入分析上下文和 API，经过代表性标的核对后才允许加入评分。
6. **评分接入**：定义适用模型、方向、归一化、异常值、缺失值、权重和最小样本。
7. **回归验证**：运行解析、指标、API、LLM 上下文和前端类型测试，并更新本文档。

### 目标数据流

```text
Eastmoney / CSI / Fund data source
  -> typed data adapter
  -> source status and freshness diagnostics
  -> normalized factor observations
  -> asset and industry profile router
  -> valuation / quality / product scorers
  -> confidence engine
  -> optional attractiveness projection after backtesting
  -> analyzer result
  -> API / Supabase history / LLM context / frontend
```

LLM 只接收最终结构化结果和诊断，不参与模型路由、数据补全、因子归一化或分数计算。

### 目标结果契约

估值 V2 新增顶层 `assessment`，使估值、基本面质量、产品质量和置信度保持语义独立。迁移期间保留现有 `valuation.score`、`level`、`level_zh`、`confidence` 和 `factors` 作为纯估值兼容字段。

```json
{
  "assessment": {
    "schema_version": "2",
    "model_version": "valuation-v2",
    "profile": "generic_non_financial",
    "analysis_as_of": "2026-07-21",
    "dimensions": {
      "valuation": {"score": 42.0, "confidence": 0.85, "factors": []},
      "quality": {"score": 71.0, "confidence": 0.50, "factors": []},
      "product": null
    },
    "overall_confidence": 0.50,
    "attractiveness": null,
    "confidence_detail": {
      "components": {},
      "caps": [],
      "reasons": []
    },
    "data_quality": {
      "sources": [],
      "warnings": [],
      "source_as_of": null,
      "retrieved_at": null
    }
  },
  "valuation": {
    "score": 42.0,
    "level": "slightly_undervalued",
    "confidence": 0.85
  }
}
```

股票的 `product` 固定为 `null`，不能用交易活跃度伪造产品质量。基金和 ETF 的 `quality` 表示底层资产质量，`product` 表示基金载体质量。各维度分别计算置信度，`overall_confidence` 使用保守聚合，不能用高置信度维度掩盖低置信度维度。

### 因子结果契约

每个正式因子至少输出以下信息：

| 字段 | 含义 |
| --- | --- |
| `key`、`name`、`category` | 唯一标识、展示名称和所属维度 |
| `value`、`unit`、`source_as_of` | 原始值、单位和数据日期 |
| `score`、`direction` | 标准化分数和高低方向 |
| `normalization` | 历史分位、行业分位或行业专属锚点 |
| `weight`、`effective_weight` | 模型权重和缺失处理后的实际权重 |
| `sample_size`、`coverage` | 样本量和数据覆盖率 |
| `source`、`status`、`eligible` | 来源、可得状态和是否满足计分条件 |
| `warnings` | 负值、异常值、样本不足或口径限制 |

只有 `status=available`、`eligible=true` 且满足最小样本的因子可以计分。`not_applicable` 不扣覆盖率，`missing/stale/error` 必须影响覆盖率或置信度。数据存在但样本不足时保持 `status=available`、设置 `eligible=false`，避免混淆“拿不到数据”和“暂不适合评分”。

### 评分基础规则

以下规则先作为实现约束，阈值在回测阶段校准：

- PE、PCF 等只在正值时参与“便宜程度”分位；亏损不能因为负 PE 被判为低估。
- PB、PS 也要求有限正值；异常值先按可解释规则过滤，不能由单次极值主导分数。
- 历史估值分位至少需要约 `252` 个交易日，约 `1200` 个交易日达到完整历史样本置信度。
- 同板块横截面使用东方财富估值板块代码；有效样本少于 `10` 时只展示原始值，不形成分位。`ORIG_BOARD_CODE` 是同板块旧代码别名，不是一级行业代码，不能用于父级回退。
- 财务质量优先使用至少 `3` 个年度报告观察稳定性；不足时可展示最新值，但降低置信度。
- 分红稳定性至少使用 `3` 个完整年度；仅有近 12 个月股息率时不声称“持续分红”。
- ETF 跟踪误差至少需要 `60` 个重叠交易日，达到 `252` 日后才视为完整年度样本。
- 缺失因子允许在同一维度内重新归一化权重，但必须满足该模型的核心因子门槛和最低有效权重。
- PEG 暂不进入正式核心评分，避免负增长、预测口径和极值造成错误排序。

### 置信度规则

置信度必须独立于资产好坏，由以下组件共同决定：

1. 数据源请求是否成功。
2. 数据日期是否满足对应数据类型的新鲜度要求。
3. 核心因子和总因子覆盖率。
4. 历史、行业和财务样本量是否充分。
5. 基金跟踪关系与持仓路由是否完整。
6. 数据是否存在冲突、异常值或代理方法。

每个维度单独计算置信度，总置信度采用保守聚合并允许设置上限：指数价格位置代理不能获得与真实指数估值相同的置信度；仅前十大持仓的主动基金不能获得完整组合估值的高置信度；跟踪关系未解析时估值置信度应为 `0`。

### 时间与版本语义

- `analysis_as_of`：本次分析允许使用数据的截止日期。
- `source_as_of`：行情、财报、持仓或基金资料本身的数据日期。
- `retrieved_at`：系统实际获取该数据的时间。
- `schema_version`：API 结果结构版本。
- `model_version`：评分配置、归一化和阈值版本。

新鲜度必须相对 `analysis_as_of` 判断，不能固定相对当前日期，否则历史分析和回测会错误使用未来信息或把当时有效的数据判为过期。

### 模型路由顺序

模型路由必须是确定性代码，并输出选择理由：

1. 先区分股票、主动基金、指数基金、ETF 和 ETF 联接基金。
2. 股票按行业优先路由到银行、保险、券商或通用非金融模型。
3. 通用非金融模型再附加红利、成长、周期等风格标签，但标签不能覆盖行业模型。
4. 基金先解析跟踪指数和目标 ETF，再决定底层资产估值与产品质量路径。
5. 无法可靠分类时使用通用模型并降低置信度，不由名称关键词强行选择高风险专属模型。

## 估值 V2 开发批次

### V2-0：链路盘点与实施设计

状态：**已完成**

- [x] 盘点数据适配器、类型、分析器、评分、API、LLM 和前端依赖。
- [x] 重跑后端基线测试和 Ruff。
- [x] 确定顶层 `assessment`、旧字段兼容边界和维度级置信度。
- [x] 确定因子准入、样本门槛、时间语义和首个开发批次。

### V2-1：行业横截面数据

状态：**已完成**

- [x] 新增按 `BOARD_CODE + TRADE_DATE` 获取完整同板块估值快照的类型和适配器。
- [x] 验证 `ORIG_BOARD_CODE` 是旧代码别名，取消不可靠的一级行业回退。
- [x] 复用 SQLite URL 缓存，相同板块和交易日使用相同缓存键。
- [x] 输出目标股票、板块样本量、有效 PE/PB 样本、排除统计和横截面分位。
- [x] 排除负数、非有限和缺失估值倍数，并保留不可计分原因。
- [x] 只接入股票分析和 LLM 结构化上下文，不改变生产估值分数或前端。

文件范围：`types.py`、`data/eastmoney.py`、`agent/market_agent.py`、`valuation/analyzer.py`、解析与指标测试。

验收结果：白酒、银行、保险、券商、电池、光伏和电力真实请求通过；保险板块5家公司不输出分位；负PE目标不参与PE分位；`132 passed, 6 skipped`，Ruff通过。

### V2-2：补充因子数据与来源诊断

状态：**已完成**

- [x] 扩展 ROIC、年度 FCFF、银行、保险和券商财务类型与解析。
- [x] 解析基金管理费、托管费、销售服务费和 `ENDNAV/FEGMRQ` 期末净资产。
- [x] 输出 `analysis_as_of/source_as_of/retrieved_at`、来源状态、缺失字段和降级原因。
- [x] 按公告日排除分析日期之后才发布的财务数据，区分正常、部分、过期、无数据、错误和无效状态。
- [x] 新字段只进入分析与 LLM 结构化上下文，并明确 `scoring_eligible=false`，未修改评分权重。

验收标准：通用非金融、银行、保险、券商和基金产品各有正常、空值、过期和错误夹具；评分代码不读取 `raw`；尚未验证覆盖率的字段不计分。

验收结果：贵州茅台、招商银行、中国平安、中信证券和 510300 真实缓存响应通过；五类数据均覆盖正常、空值、过期和错误夹具；`138 passed, 6 skipped`，Ruff 通过。

### V2-3：评分基础设施

状态：**已完成**

- [x] 新增独立的版本化评分配置、归一化、置信度和结果契约模块，旧 `framework.py` 保持兼容职责。
- [x] 实现统一因子状态、正值过滤、样本门槛、核心因子门槛、最低有效权重和权重重归一化。
- [x] 区分 252 日最低可计分样本和 1200 日完整历史置信度样本。
- [x] 增加顶层 `assessment`、维度级置信度、`schema_version/model_version`、来源诊断和 `attractiveness=null`。
- [x] 股票估值维度使用新内核；基金和指数代理先标准化旧因子契约，保留兼容来源标识。
- [x] 保留旧 `valuation.score/level/confidence/factors`，当前前端和历史记录可以继续读取。

验收标准：边界、负值、缺失、过期、样本不足、时间截点和权重覆盖测试齐全；相同输入和模型版本产生可复现结果。

验收结果：新增 16 个评分基础设施测试；真实 600519 新旧估值分同为 `16.69`；`154 passed, 6 skipped`，Ruff 通过。

### V2-4：股票行业模型

状态：**待开始**

- 实现通用非金融、银行、保险和券商第一版模型。
- 新增独立基本面质量分，估值分仍只描述贵贱。
- 红利、成长和周期先作为通用非金融附加标签，待回测后决定是否独立模型化。
- 输出估值与质量维度及各自置信度，不输出综合吸引力分。

验收标准：模型路由确定且可解释；每个模型都有核心因子门槛；质量变化不能改写原始估值等级。

### V2-5：基金与 ETF 产品模型

状态：**待开始**

- 使用 V2-2 已验证的费率、规模数据，并计算跟踪误差和跟踪偏离。
- 在可靠指数估值源接入前继续标记价格位置代理。
- 将底层资产质量、产品质量和持仓/指数路由置信度分开。
- 不输出尚未回测的综合吸引力分。

验收标准：ETF、ETF 联接、普通指数基金和主动基金走不同路径；前十大持仓不能伪装成完整持仓。

### V2-6：API、前端与 LLM 迁移

状态：**待开始**

- 扩展 API 与 TypeScript 类型，展示三类评分维度及维度级、总体置信度，保留旧历史结果兼容。
- LLM 上下文接收四维结果、因子、日期、来源和诊断。
- 固定模板和 LLM 均使用“价格位置代理”等准确术语。
- `attractiveness` 在回测前保持 `null`，前端不展示占位评级。

验收标准：前端 lint/build 通过；API、流式聊天和历史记录测试覆盖新旧两种结果。

### V2-7：回测与模型校准

状态：**待开始**

- 建立无未来数据泄漏的历史截面与持有期回测。
- 按行业和市场阶段评估分层收益、回撤、换手和稳定性。
- 用结果校准估值、质量和产品模型的权重、阈值和置信度上限。
- 单独验证综合吸引力候选公式及等级，不通过则保持 `attractiveness=null`。
- 产出回测报告和候选版本化模型参数，不在本批次自动发布。

验收标准：任何正式权重调整都有回测报告、样本区间和模型版本；回测失败的实验因子不得进入生产评分。

### V2-8：综合吸引力发布

状态：**待开始，受 V2-7 结果约束**

- 仅在回测支持时发布综合吸引力权重、等级和模型版本。
- API、前端和 LLM 同时展示原始维度、总体置信度和综合吸引力，禁止只展示单一总分。
- 回测不支持时取消该指标，不为了完成路线图强行上线。

验收标准：发布参数与回测报告一致；低置信度结果按已验证规则向中性收缩；不生成买入或卖出指令。

## 决策日志

### 2026-07-21：估值数据与评分边界

- 核心策略必须建立在已验证可获得的数据之上，不为理想模型虚构数据。
- 最终输出采用估值分、基本面质量分、产品质量分和置信度四个维度。
- 成长归入基本面质量，不单独重复计分。
- “综合吸引力分”是“调整分”的候选名称，回测通过前不发布，也不能替代原始维度。
- 向中性值收缩是待回测假设，不改变原始估值事实。
- EV/EBITDA、研发效率、保险 PEV 等当前不可稳定获得的指标不进入第一版核心模型。
- 指数价格分位必须标记为价格位置代理，不能称为真实指数估值。

### 2026-07-21：基金数据路由

- ETF 联接基金优先解析跟踪指数和目标 ETF。
- 优先使用官方指数成分数据，其次使用目标 ETF 披露持仓。
- 跟踪关系无法解析时关闭错误兜底，避免把联接基金自身低比例股票持仓当作底层指数持仓。

### 2026-07-21：估值 V2 实施架构

- 估值优化采用数据、诊断、评分、展示分阶段接入，不直接在旧公式上叠加新权重。
- 新因子必须经过实时探测、测试夹具、明确类型、质量诊断、只读展示和评分接入流程。
- V2 结果采用向后兼容结构，迁移期间保留现有估值字段。
- 四维结果放入顶层 `assessment`，各评分维度分别计算置信度。
- 当前 `framework.py` 后续按评分配置、归一化、置信度和基金聚合职责拆分。
- 首个开发批次只实现完整行业横截面数据和分析上下文，不改变生产估值结论。
- “估值 V2 开发批次”是唯一进度清单，不再维护重复的阶段路线图。
- 综合吸引力只能在回测后通过独立发布批次上线。

### 2026-07-21：V2-1 行业横截面路由

- 东方财富估值记录中的 `BOARD_CODE` 可以按交易日返回完整同板块横截面。
- `ORIG_BOARD_CODE` 返回相同公司集合，是旧代码别名，不是一级行业代码。
- 不建立推测性的父级行业映射；有效正值样本少于 `10` 时不输出分位。
- PE、PB 分别判断计分资格，负 PE 不会被解释为低估，仍可独立计算有效 PB 分位。
- 行业横截面目前只用于单只股票分析上下文，不在基金持仓并发分析中逐股请求。

### 2026-07-21：V2-2 补充因子与来源诊断

- 东方财富年度财务响应按 `ORG_TYPE` 确定性区分通用、银行、保险和证券字段，不跨行业填充缺失值。
- `FCFF_BACK/FCFF_FORWARD` 仅作为年度源字段保存；尚无可靠 TTM 语义，因此不计算自由现金流收益率。
- 财务时间截点同时检查报告期和公告日，分析日期之后才公告的数据会被排除。
- 基金规模明确使用详情接口的 `ENDNAV` 期末净资产，并以 `FEGMRQ` 作为来源报告日，不描述为实时规模。
- 新因子统一输出来源、分析日、源报告日、获取时间、缺失字段和降级原因，评分资格保持关闭。

### 2026-07-21：V2-3 评分基础设施

- V2 结果使用 `schema_version=2` 和 `model_version=valuation-v2.0.0-infrastructure`，相同版本和输入必须产生相同输出。
- 因子状态统一为 `available/missing/stale/error/invalid/not_applicable`；数据存在但样本不足时保持 `available` 且 `eligible=false`。
- 股票 PE、PB、PS、PCF 只接受有限正值，最低历史样本为 252，达到 1200 才获得完整样本置信度。
- PE 和 PB 是当前通用股票估值维度的核心因子；核心因子缺失或有效权重低于 50% 时，新维度不计分。
- 缺失因子在维度内重归一化有效权重，原始权重和实际权重同时输出；`not_applicable` 不降低覆盖率。
- 维度置信度由来源、时效、因子覆盖、数据覆盖、样本充分度、路由和方法质量独立计算，并允许代理方法设置上限。
- 顶层总体置信度采用有效维度中的最小值；当前质量分、产品质量分和综合吸引力仍为 `null`，等待后续模型批次。
- 迁移期间保留旧估值字段；基金旧因子带 `legacy_compatibility_factor` 标记，不伪装成已完成的新产品模型。

## 文档维护规则

每次后续工作都应遵循以下流程：

1. 开始任务前阅读本文档，确认当前阶段、既有决策和下一项工作。
2. 开发过程中如发现数据源、字段语义或方案假设有误，先修正文档中的事实。
3. 完成任务后更新当前能力、V2 工作包状态、验证结果和决策日志。
4. 规划项和已完成项必须分开书写，不得把“接口存在”描述为“功能已实现”。
5. 提交代码时，文档更新应与对应功能处于同一提交或紧随其后的文档提交。
